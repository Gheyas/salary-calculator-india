{
  "globalHeaders": {
    "Cache-Control": "no-cache"
  },
  "routes": [
    { 
      "route": "/assets/*.{js,css,png,jpg,jpeg,gif,svg,woff,woff2}", 
      "headers": { 
        "Cache-Control": "max-age=31536000, immutable" 
      } 
    }
  ]
}
